import type { LucideIcon } from 'lucide-react'

import { appIcons } from '@/config/app-icons'

import type { ListIconKey } from '../types/list.types'

export const listIcons: Record<ListIconKey, LucideIcon> = appIcons
