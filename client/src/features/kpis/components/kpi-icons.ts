import type { LucideIcon } from 'lucide-react'

import { appIcons } from '@/config/app-icons'

import type { KpiIconKey } from '../types/kpi.types'

export const kpiIcons: Record<KpiIconKey, LucideIcon> = appIcons
